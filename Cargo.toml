cargo-features = ["codegen-backend"]

[workspace]
members = [
    "monmon-impl",
    "monmon-dbg",
]
default-members = ["monmon-dbg"]

resolver = "2"

[profile.dev]
debug = "full"
opt-level = 0
codegen-backend = "cranelift"

